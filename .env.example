# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# Site
VITE_SITE_NAME=La Grandezza Events

# Admin (set your own password via the Admin Settings panel after first login)
# The default password is set in AuthContext — change it immediately on first run.
